#include <stdio.h>

int main() {
    int a[] = {8, 3, 15, 6, 2};
    int min = a[0];

    for (int i = 1; i < 5; i++) {
        if (a[i] < min)
            min = a[i];
    }

    printf("Minimum element: %d\n", min);
    return 0;
}