#include <stdio.h>

int main() {
    int a[] = {5, 12, 7, 3, 9};
    int max = a[0];

    for (int i = 1; i < 5; i++) {
        if (a[i] > max)
            max = a[i];
    }

    printf("Maximum element: %d\n", max);
    return 0;
}